const std = @import("std");

const Game = struct {
    name: [_]u8,
    appid: [_]u8
};

fn checkGamePresent(game: ) {

}

pub fn main() void {
  // clear login status
  // login
  // fetch list of games from nix
  // query steam API with janet
  // filter list for games that are set to be installed
  // uninstall games that are installed in steam not in that list
  // start installs for games on that list
}
